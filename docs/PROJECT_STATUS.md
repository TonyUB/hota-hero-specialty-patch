# 项目状态与接手结论

## 已确认事实

1. `Patch_v1.8` 是唯一可信的修改基线；v1.9、v2.0、v2.1、v2.2、v2.3 只能用于失败取证，不能继续开发或复用 Cure Hook。
2. 原始 `Patch_v1.8.zip` 的 SHA-256 与交接值一致，包内 12 个文件也全部逐项一致。
3. 当前补丁必须继续保留：
   - 埃尔芙槽位、25/25/25 仙灵初始兵力、仙灵/妖精伤害与速度各 +1；
   - 伊沃 12–24 半人马以及固定 6 + 6 木精灵；
   - 阿德拉原生祝福特长，并仅让阿德拉施放祝福时最终消耗为 0；
   - 梅洛迪娅、索姆拉、罗尼斯的 HotA 1.8.0 原生行为；
   - 215 帧 D32F `UN44.DEF` / `UN32.DEF` 及埃尔芙第 141 帧。
4. 过去失败的主要原因是把静态候选地址当作真实运行路径。`Patch_v2.4_diag01` 的 15 条实机记录现已证明磁盘 EXE 的单体/群体 Cure 包装器在真实运行环境中执行。
5. 纯净 EXE → `Patch_v1.8` 的完整差异已确定：标准版与 HD 版均为 80 个差异字节、17 个精确区间、6 个逻辑分组，两套修改完全一致。
6. `HotA.dll` 有 3 处直接调用 `GetResurrectionTarget`、2 处直接调用 `ResurrectTarget`；后两处均传入 `temporary=0`，确认原生永久复活接口的地址和调用方式。

## 当前唯一功能目标：Stage 2

只处理仍有至少一个单位存活的兵队：

1. 保留原生 Cure 的治疗、解除负面状态和英雄特长缩放。
2. 先完成正常治疗。
3. 仅将实际剩余治疗量交给原生 Resurrection 路径。
4. 复活永久有效，且必须复用原生 Resurrection 的目标资格限制。
5. 不手工维护亡灵/元素/构装体黑名单，不手工写 `numberAlive`、`healthLost` 或 `numberForeverDead`。
6. 阿斯特拉的独立数据修改是初级智慧术 + 初级水系魔法。

Stage 2 不处理全灭尸体、不扩展高级水系群体 Cure 的尸体扫描，也不做相关 UI、AI 或提示。

## 当前证据等级

- 基线 ZIP 哈希：已验证
- 包内文件哈希：已验证
- 纯净运行时文件哈希：已验证；`HotA_Settings.ini` 因含本机用户路径仅保留本地与注释哈希，不提交
- `Patch_v1.8` 与纯净 1.8.0 的差异地图：已完成，标准/HD 修改集合完全一致
- 标准版/HD Cure 路径：静态位置与字节一致；用户日志已证明所用启动版本运行时命中，但日志未记录具体 EXE
- HotA/HD DLL 静态覆盖扫描：已完成；运行日志证明没有绕过两个 Cure call
- 原生永久复活接口：`HotA.dll` 直接调用证据已确认
- `Patch_v2.4_diag01`：15 条日志通过；两名英雄均命中单体和群体，溢出计算全部一致
- `Patch_v2.4_STAGE2_TEST`：已构建并完成静态/回滚/ZIP 校验；游戏内复活效果未验证
- Stage 2 正式功能：尚未发布

当前最重要的静态发现：原生 CureCore 返回时，`EAX < 0` 的绝对值就是已经包含原生英雄特长增幅的治疗溢出量。候选实现将单体/群体两处 Cure `call` 重定向到统一包装器，先调用原生 Cure，再复用 `GetResurrectionTarget` 与 `ResurrectTarget(..., temporary=0)`；但必须先通过诊断包装器证明运行时命中。

## 下一步执行顺序

1. 用户安装 `Patch_v2.4_STAGE2_TEST`，让尤兰德与阿斯特拉分别对“仍存活且已有阵亡”的兵队测试单体/群体治愈。
2. 记录施法前后单位数，并确认战斗结束后新增单位是否保留。
3. 回传 `hota_cure_stage2.log`；`revived=Y` 应只出现在原生资格验证通过的溢出场景。
4. 验证满编仅受伤兵队、其他英雄和原生禁止复活目标均保持原行为，再决定是否形成正式 Stage 2 版本。
