# Analysis workspace

开始游戏逻辑补丁前必须完成以下交付物：

- [ ] `baseline_diff.md`：纯净 1.8.0 与 `Patch_v1.8` 的完整差异、PE 节区和回滚字节
- [ ] `existing_patch_map.json`：现有修改、代码洞、跳转和数据占用的机器可读地图
- [ ] `cure_runtime_path.md`：标准版/HD 版 Cure 调用图、参数、寄存器和证据等级
- [ ] `runtime_traces/`：能够证明真实路径执行的日志或调试证据
- [ ] `call_graphs/`：EXE、HotA DLL、HD DLL 与 patcher 之间的调用/覆盖关系

当前仅建立目录与待办骨架；这些成果尚未完成。
