# LOGDIAG01 实机验证

状态：**一次性诊断门禁已完成；TEST12 的向量轮转没有丢失，屏幕顺序来自另一层按追加时序维护的显示缓存。**

- 用户文件：`hota_cure_logdiag01.bin`
- SHA-256：`570280a74232e0b643ba2bccc65d85e300a026fe268426b45a1a70fc9b7d8779`
- 文件大小：504 字节
- 定长记录：21 条，每条 24 字节
- 截图结果：4 条“起死回生”记录之后才显示“阿斯特拉施放治愈”

## 事件计数

| 事件 | 数量 |
|---|---:|
| `wrapper_init` | 10 |
| `corpse_helper_init` | 2 |
| `resurrection_entry` | 4 |
| `native_post_enter` | 3 |
| `after_rotation` | 1 |
| `after_refresh` | 1 |

## 关键记录

第二次群体治愈的四次治愈专属复活依次把状态从 `0x81` 累加到 `0x84`，目标指针分别为 `0x0D3954CC`、`0x0D395A14`、`0x0D3969EC`、`0x0D39747C`。

原生治愈日志追加后的事件 4：

```text
state=0x84
vector_begin=0x16E44F40
vector_end=0x16E44F8C
cure_pointer=0x16E41B80
```

轮转后的事件 5：

```text
count=4
insert_position=0x16E44F78
insert_value=0x16E41B80
tail_value=0x16E44EF0
```

原生刷新后的事件 6 与事件 5 完全一致：插入位置仍保存 `cure_pointer=0x16E41B80`，尾值也没有被改回。

## 结论与 TEST13 方向

TEST12 的运行路径、复活计数、日志指针轮转和 `0x00472770` 刷新全部实际执行且结果正确。截图仍然后置，说明 HotA/HD 的可见日志还维护了一层按 `0x004729D0` 实际调用先后生成的显示缓存，事后移动原生向量无法改变该缓存。

因此 TEST13 不再轮转已追加的指针，而是在群体治愈期间暂存每队复活日志参数：先让原版治愈记录真正追加，再复用原版格式串与 `0x004729D0` 依次追加暂存的复活记录。单体治愈和普通转世重生保持原调用时序。
