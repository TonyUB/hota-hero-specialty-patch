# Patch_v1.8 基线差异

状态：**待分析**

已修改基线 SHA-256 已核验。当前缺少匹配的纯净 HotA 1.8.0 `h3hota.exe` 与 `h3hota HD.exe`，因此尚不能列出可信差异区间、原始字节或回滚字节。

完成本文件时必须包含：

1. 两个 EXE 的 PE 节区表。
2. 每个连续差异区间的文件偏移、VA/RVA、原始字节、修改字节和回滚字节。
3. 现有跳转、代码洞和新增数据占用。
4. 埃尔芙、阿德拉、初始兵力及资源变更的归属说明。
5. 标准版与 HD 版独立结果。
