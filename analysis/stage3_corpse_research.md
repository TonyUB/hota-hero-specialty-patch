# Stage 3：治愈复活全灭尸体

状态：**全部实机门禁通过；`Patch_v2.5_STAGE3_TEST3` 已提升为正式版 `Download/Patch_v2.5.zip`。**

## 启动故障复盘

原测试包把第二代码洞错误地从 `0x00639C28` 开始。这个字节虽然是零，却不是可用填充：它是运行时传给 `GetProcAddress` 的导出名 `MainProc\0` 的终止符。覆盖后，启动代码会把后续机器码也当成导出名的一部分，`GetProcAddress` 返回空指针，随后在 `0x00639C17` 的 `call eax` 处失败。因此故障发生在 HotA 主入口执行之前，与存档和玩法逻辑无关。

修正版把载荷起点移到 `0x00639C29`，并在构建前后逐字节验证 `0x00639C20` 处的 `MainProc\0` 保持不变。原故障包、说明和清单只保留在 `failed_forensics/`，不得安装。

## TEST2 实机结果与剩余根因

用户在标准战斗中分别测试了尤兰德、阿斯特拉、亡灵和非特长英雄：

- 两名特长英雄对尚未全灭的兵队使用单体治愈，仍可按 Stage 2 溢出量复活且战后永久保留；
- 两名英雄取得高级水系后，群体治愈均可复活多个全灭尸体，且战后永久保留；
- 亡灵只接受原治疗，不会复活已死亡单位；
- 非特长英雄的单体和群体治愈都不会复活全灭兵队；
- 唯一失败项是两名特长英雄对合法全灭尸体使用单体治愈时，战斗日志显示“抵抗魔法”。

这证明目标解析和 `0x005A3D26` 的可选性门禁已经成功，否则单体施法不会进入结算。正式单体结算在 `0x005A05FA` 又直接调用一次 `0x005A83A0`，参数仍是 Cure `0x25`；该原生活体效果检查面对 `numberAlive == 0` 返回零，随后在 `0x005A0622` 进入“抵抗魔法”分支。群体尸体扫描不经过这段单体结算代码，因而已经正常工作。

`TEST3` 在 `0x005A05E1` 增加一个严格门禁：仅当法术是 Cure、目标完全阵亡、施法英雄为尤兰德/阿斯特拉，并且 `GetResurrectionTarget(side, corpseHex, 0)` 再次返回同一兵队指针时，才跳到 `0x005A0628`，避开不适用于尸体的 Cure 活体效果/抵抗检查。其他情况逐条重放原指令并继续原版 `0x005A05E8` 路径。

## 根因

治愈的法术 ID 为 `0x25`。原版目标解析函数 `0x005A3C60` 对治愈固定调用战场格的活体解析 `0x004E7230`；只有 `0x26` 和部分 `0x28` 复活法术会进入 `GetResurrectionTarget` `0x005A3FD0`。所以 Stage 2 能复活“仍有单位存活”的兵队，却无法让光标选择全灭尸体。

原版并未丢失尸体信息。每个战场格保留尸体所属阵营、原兵队槽位和重叠状态；`0x005A3FD0` 会据此还原原兵队指针，并调用 `0x005A83A0` 按原生转世重生规则检查阵营、亡灵/元素/构装等资格、占格冲突和复活上限。

## 单体目标路径

在 `0x005A3C77` 接入目标解析门：

1. 治愈仍先执行原活体解析，活体目标行为完全不变。
2. 只有英雄施法、目标阵营等于当前施法方、当前英雄为尤兰德 `0x19` 或阿斯特拉 `0xAA`，且活体解析为空时，才调用 `GetResurrectionTarget(side, hex, context=0)`。
3. `0x005A3D26` 只对已经由上述原生复活解析确认、并且 `numberAlive == 0` 的治愈目标直接判定可选，避免再次调用只适用于活体的 Cure 效果校验。
4. 非特长英雄、战斗生物施法和普通活体治愈继续走原版分支。

## 全灭兵队结算

全灭兵队不能调用 `CureCore`：该函数会访问活体生命损失、状态清除和当前单位数相关字段，但不会把 `numberAlive` 从零恢复。

测试版按 CureCore 的原生公式计算完整治愈量：

```text
amount = CureBase[waterMastery] + CurePowerCoefficient * spellPower
amount += hero->NativeSpellSpecialtyBonus(Cure, targetLevel, amount)
```

随后再次要求 `GetResurrectionTarget` 返回的指针与当前尸体兵队完全一致，再调用：

```text
ResurrectTarget(target, amount, temporary=0)
```

不直接写 `numberAlive`、`healthLost`、`numberForeverDead` 或其他兵队状态字段。

## 高级水系群体路径

原群体治愈在 `0x005A1B30–0x005A1BEC` 遍历当前阵营兵队槽位并处理活体。测试版在该循环结束后的 `0x005A1BEC` 接入第二次扫描：

- 仍使用 `[manager + side*4 + 0x54BC]` 的当前阵营槽位数；
- 每个战斗兵队结构大小为 `0x548`，阵营每侧预留 21 个槽位；
- 只考虑 `numberAlive == 0` 且初始单位数大于零的记录；
- 使用尸体自身的战场格再次调用 `GetResurrectionTarget`，并要求返回指针完全一致；
- 每个合格尸体使用完整治愈量永久复活；
- 已复活槽位加入原群体治愈的受影响标记数组，随后继续执行原版群体动画/日志路径。

活体兵队先按 Stage 2 完成正常治疗和溢出复活；尸体扫描发生在其后。多个尸体或活体占用同一落位时，以原生尸体解析和占格规则为准。

## 挂钩与代码洞

| 位置 | 作用 |
|---|---|
| `0x005A1B05` | 单体 Cure 调用统一包装器 |
| `0x005A1BB4` | 群体活体 Cure 调用统一包装器 |
| `0x005A1BEC` | 群体活体循环结束后扫描尸体 |
| `0x005A05E1` | 单体正式结算再次确认原生复活资格后，跳过尸体不适用的 Cure 活体效果/抵抗检查 |
| `0x005A3C77` | Cure 活体优先、尸体回退目标解析 |
| `0x005A3D26` | 对已经原生确认的 Cure 尸体跳过活体二次校验 |
| `0x00639D80–0x00639FFC` | 治愈量计算、统一包装器、目标门和单体结算门；636 字节 |
| `0x00639C29–0x00639CF5` | 群体尸体扫描；204 字节；保留前一字节的 `MainProc\0` 终止符 |

两个代码洞在标准版与 HD 版的可信 `Patch_v1.8` 中均逐字节确认可用。第二代码洞特意跳过相邻启动字符串的终止符，并以独立保护断言保证该终止符不被修改。

## 构建与静态验证

`tools/build_stage3_patch.py` 只接受 `baselines/Patch_v1.8_SHA256.txt` 匹配的 12 文件基线。构建器验证：

- 两个 EXE 的六个原始挂钩字节完全匹配；
- 六个补丁分支反汇编后均到达预期载荷入口；
- 两个 EXE 大小不变，其他 10 个包内文件哈希不变；
- 载荷包含原生 CureCore、英雄法术特长、活体解析、尸体解析和永久复活接口；
- 每个修改区都能完整回滚重建输入 EXE；
- ZIP 成员集合和 CRC 完整通过。

输出：

```text
Patch_v2.5_STAGE3_TEST3.zip
SHA-256 11d3d34175b8b15b5de7f8c5af31e9809762cdb76909910704936e2b43fd8c73

h3hota.exe
SHA-256 1ff5c69229dce0e617ae8b731a85d816a2cf3b3911a946fd93a71c914e3cefc1

h3hota HD.exe
SHA-256 704a8bd314233a3031899bc6f72446bfdc927f8d75ebab34a3660081d4353288
```

## 实机门禁

最终结果：**全部通过。** 尤兰德、阿斯特拉的单体与高级水系群体尸体复活、存活兵队溢出复活、亡灵和非特长英雄负例、战后永久性均已确认。重叠尸体按原生规则只复活一个；尸体格被存活兵队占用时无法复活。

1. [已通过 TEST3] 尤兰德、阿斯特拉都能用单体治愈选中并复活合法的全灭尸体。
2. [已通过 TEST2/TEST3] 两名英雄的高级水系群体治愈都能同时处理活体和多个合法尸体。
3. [已通过 TEST2/TEST3] 亡灵不会被治愈复活已死亡单位。
4. [已通过 TEST2/TEST3] 非特长英雄不会获得单体或群体尸体复活。
5. [已通过 TEST3] 复活数量、动画、战斗日志、重叠尸体与占格冲突均符合原生规则。
6. [已通过 TEST2/TEST3] 新增单位在战斗结束后永久保留。
