# Cure 真实运行路径

状态：**未验证**

交接文档中的 H3API 地址和历史地址只能作为搜索锚点，不能作为 HotA 1.8.0 运行时事实。

待回答：

1. 英雄主动施放 Cure 时最先进入哪个模块？
2. 最终治疗量在哪个函数和参数/寄存器中确定？
3. 乌兰德与阿斯特拉的特长缩放在哪一步加入？
4. 目标生命和数量状态在哪里写入？
5. 标准版与 HD 版是否共用路径？
6. `HotA.dll`、`HD_HOTA.dll`、`HW_HOTA.dll` 或 `patcher_x86.dll` 是否覆盖候选 EXE 路径？

证据等级应区分：静态候选、交叉引用支持、动态断点命中、诊断日志实机确认。
