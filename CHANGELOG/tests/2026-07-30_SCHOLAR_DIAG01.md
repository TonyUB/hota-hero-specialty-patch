# 2026-07-30 科洛尼斯学术特第一阶段诊断包

## 构建

- 构建名：`HOTA_NEW_HERO_V1.2_SCHOLAR_DIAG01`
- 正式基线：`HOTA_NEW_HERO_V1.14`
- 基线 ZIP SHA-256：`8077624b88dc83762b77c34cb8645a4907cf2f3bc0538ae0684da713edd4ed85`
- 诊断 ZIP SHA-256：`c5f74166efc74042f49149d7c92f29bf9975dcadd210a363bc324312055a3df1`
- 运行日志：`hota_scholar_diag01.bin`
- 正式发布：否

## 运行路径

标准版与 HD 版均命中原生学术交换入口 `0x004A25B0`。入口使用 `ECX` / `EDX` 传入双方英雄指针；函数内读取 `+0xDB` 学术、`+0xD0` 智慧术和 `+0x3EA` 的 70 项已学法术表，并执行原生魔法书检查、双向等级截断、法术过滤、学习和提示。

诊断 Hook 仅在科洛尼斯（英雄 ID `24`）参与会面时写日志；重放原始序言：

```text
原始/回滚：55 8B EC 6A FF 68 78 B7 62 00
替换：      E9 CB 5B 24 00 90 90 90 90 90
继续地址：  0x004A25BA
诊断入口：  0x006E8180
```

两个 EXE 使用完全相同的 4096 字节诊断载荷，载荷 SHA-256 为 `a2ebb7839bc83841218b201066ce4afdd7995928852c7c6322afbf61b86d47ec`。

## 图标

- 使用原生 `Secskill.def` / `SECSK32.def` 的第 56 帧，即 `skill18c.pcx` / `skl3218c.pcx`；
- 明确对应 `Expert Scholar`（高级学术），未使用第 55 帧 `Advanced Scholar`（中级学术）；
- D32F `UN44.DEF` / `UN32.DEF` 仅替换科洛尼斯第 24 帧像素区；
- 新增标准界面覆盖 `Data/HPS024DR.PCX`；
- D32F 其余 214 帧和所有非目标字节均保持不变，并验证完整回滚。

## 文件边界

相对正式 V1.14：

- 修改 `h3hota.exe`、`h3hota HD.exe`；
- 修改 `_HD3_Data/Compability/#hota15/UN44.DEF`、`UN32.DEF` 的第 24 帧；
- 修改根目录 `安装说明.txt`；
- 新增 `Data/HPS024DR.PCX`；
- 其他正式文件逐字节继承 V1.14。

## 静态验收

- 两个来源 EXE 固定哈希：通过；
- Hook 源字节、目标地址与原始序言重放：通过；
- 独立第六 PE 节位于原映像边界：通过；
- 标准版 / HD 版诊断载荷一致：通过；
- PE 校验和：通过；
- EXE 完整回滚重建：通过；
- D32F 单帧隔离与回滚：通过；
- ZIP CRC 与成员集合：通过；
- 独立验证脚本：通过。

## 启动冒烟

- 将候选包临时覆盖到 `C:\Game\180\180` 后，`h3hota.exe` 存活至主窗口，窗口标题为 `Heroes of Might and Magic III: Horn of the Abyss`；通过。
- 测试脚本随后逐项恢复两个 EXE、两份 D32F，并移除本次新增的 `Data/HPS024DR.PCX`；恢复后哈希与覆盖前完全一致。
- `h3hota HD.exe` 在命令行直接启动时自行退出；用已经恢复的正式 V1.14 做同条件对照也同样退出，因此不能把该现象归因于候选包。HD EXE 的来源哈希、Hook 源字节、载荷、PE 校验和和完整回滚仍已独立验证；实际 HD Mod 入口留给用户按日常方式运行确认。

## 用户运行结果与撤回结论

用户将 DIAG01 覆盖到纯净 HotA 1.8.0 后，在“单人游戏→新建场景”阶段直接闪退；没有游戏错误报告，也没有生成 `hota_scholar_diag01.bin`。

该阶段尚未发生友方英雄会面，因此没有证据表明学术入口 Hook 或字段读取已经执行。DIAG01 新增的第六 PE 节成为首要风险项：主菜单存活不足以证明 HotA/HD 后续场景初始化接受额外节布局。

结论：DIAG01 已撤回，不得继续分发或作为后续功能基线。DIAG02 改为复用 V1.14 既有 `.luck3` 的已验证空白尾部，不再新增 PE 节。
