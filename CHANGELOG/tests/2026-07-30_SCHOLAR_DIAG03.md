# 2026-07-30 科洛尼斯学术特 DIAG03

## 触发原因

用户确认 `HOTA_NEW_HERO_V1.2_SCHOLAR_DIAG02` 仍在“单人游戏 → 新建场景”阶段直接闪退，且没有生成 `hota_scholar_diag02.bin`。

该时间点尚未发生英雄会面，因此原生学术术入口 `0x004A25B0` 尚未执行。DIAG02 已经取消第六个 PE 节且保持 EXE 原大小，剩余需要优先隔离的变量是同时加入的科洛尼斯头像/特长图标资源。

## 构建

- 构建名：`HOTA_NEW_HERO_V1.2_SCHOLAR_DIAG03`
- 正式基线：`HOTA_NEW_HERO_V1.14`
- 基线 ZIP SHA-256：`8077624b88dc83762b77c34cb8645a4907cf2f3bc0538ae0684da713edd4ed85`
- DIAG03 ZIP SHA-256：`98d551e76f40ceb9fdf78a3170a01a5735ca1b306de85d7535cf376f9f9e7753`
- 运行日志：`hota_scholar_diag03.bin`
- 正式发布：否

## 单变量隔离

DIAG03 完全撤销 DIAG01/DIAG02 的所有图形资源修改：

- 不修改 `UN32.DEF`；
- 不修改 `UN44.DEF`；
- 不新增 `Data/HPS024DR.PCX`；
- 不修改任何其他 DEF、PCX 或文本资源；
- 科洛尼斯暂时继续显示正式 V1.14 原图标。

只保留两项 EXE 内部变化：

1. 原生学术术入口跳转到行为透明的诊断包装器；
2. 诊断代码写入现有 `.luck3` 节已验证全零的 `0x800..0xFFF` 尾部。

本阶段不改变实际学术术传授效果。

## 独立验证

- 候选包与正式 V1.14 的 ZIP 成员集合完全相同；
- 只改变 `h3hota.exe`、`h3hota HD.exe` 和根目录安装说明；
- 其余 11 个成员逐字节保持不变，包含全部图形资源；
- 两个 EXE 大小均保持 `2936832` 字节；
- PE 节数均保持 `5`，未改变 `SizeOfImage`；
- `.luck3` 正式前 `0x800` 字节逐字节保留；
- 标准版与 HD 版诊断尾部一致；
- Hook、PE 校验和、完整 EXE 回滚验证通过；
- ZIP CRC 与成员集合验证通过。

## 启动冒烟

候选包临时覆盖到本地 HotA 1.8.0 后，`h3hota.exe` 成功建立主游戏窗口，标题为 `Heroes of Might and Magic III: Horn of the Abyss`。测试结束后两份 EXE 和随包检查的正式资源均恢复到覆盖前哈希。

## 运行验收

等待用户依次确认：

1. “单人游戏 → 新建场景”是否可以进入；
2. 若能进入，科洛尼斯与友方英雄会面后是否生成 `hota_scholar_diag03.bin`。

若 DIAG03 仍在相同位置闪退，则图形资源可排除，下一阶段应拆分“学术入口跳转字节”和“.luck3 尾部载荷”两个变量。

## 实机结果

通过。用户成功进入场景并完成一次科洛尼斯会面，返回一条有效 `SCH1` 记录；未发生闪退。由此确认：

- 复用 `.luck3` 尾部的入口诊断稳定；
- DIAG01/DIAG02 的新图形资源组合是此前场景载入闪退的直接关联变量；
- 原生学术入口、英雄指针、ID、学术、智慧和法术表字段均可进入功能实现阶段。
