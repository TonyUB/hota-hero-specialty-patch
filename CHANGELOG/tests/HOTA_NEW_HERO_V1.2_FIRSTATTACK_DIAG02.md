# HOTA_NEW_HERO_V1.2_FIRSTATTACK_DIAG02

- 类型：第二次行为透明运行路径诊断，不是正式版或功能测试版。
- 来源：正式 `HOTA_NEW_HERO_V1.11.zip`，不以 DIAG01 为增量来源。
- 修正目标：解决 DIAG01 实际运行后不生成日志的问题。

## 与 DIAG01 的差异

1. 在已经由正式版功能验证的 `H3Hero::GetLuckBonus` 固定幸运 `+3` 包装器中增加路径 `0` 记录。马洛迪亚或黛瑞丝只要经过该路径，就能证明诊断 EXE 已加载且文件写入器可用。
2. 两个攻击投骰入口改为记录所有原生调用，不再把参战方指针解引用为英雄对象，也不按未证实的英雄编号提前过滤。
3. 路径 `1/2` 仍记录攻击者、原始参数、有效阵营、队列、兵种、当前幸运、调用者及关键战斗字段，用于区分主动近战、主动远程、反击和连续攻击。
4. 固定幸运 `+3` 的返回值、原生硬封锁顺序、被覆盖指令重放以及其余全部游戏资源保持不变。

## 当前状态

- 静态构建与完整回滚验证：通过。
- 标准版输出 EXE SHA-256：`4cc535c509aa07aad1feb30a3b9fabe5a9d467fc550d5b4a7f832c78c0e3a1a1`。
- HD 版输出 EXE SHA-256：`66f5f65c330ee391429b556959b965bf73261f493fa408bc6b6106f9e7d51394`。
- 诊断 ZIP SHA-256：`c1a98b37c13917ccbf612af92a43b674862600a26f9ff0bb14e0345e042346d1`。
- 可复现构建：连续两次构建得到相同 ZIP SHA-256。
- 启动冒烟：通过。覆盖后先核验 DIAG02 两个 EXE 哈希，隐藏启动 `h3hota HD.exe` 后观察 18 秒；进程持续存活且响应正常，运行中再次核验两个文件仍为 DIAG02 哈希。停止进程后恢复测试前的两个原始 EXE并通过哈希核验。
- 文件边界：包内仅两个 EXE 与根目录安装说明相对 V1.11 发生变化；DLL、LOD、HeroSpec、图片及全部其他资源逐文件保持一致。
- 用户运行诊断：待回传 `hota_luck_firstdiag02.bin`。
