# Baselines

- `Patch_v1.8/`：当前唯一可信的已修改基线，12 个文件的内容哈希均已验证。
- `Patch_v1.8_SHA256.txt`：原始 ZIP 与包内文件的可信哈希清单。
- `hota180_clean/`：纯净 HotA 1.8.0 输入占位目录；在取得匹配版本的文件前，不得开始差异映射或分配代码洞。

不要把 v1.9、v2.0、v2.1、v2.2 或 v2.3 放入可信基线路径。
